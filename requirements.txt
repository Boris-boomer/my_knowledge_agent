# ================================================================
# my_knowledge_agent 依赖清单
# ================================================================
# 安装命令：uv pip install -r requirements.txt
# ================================================================

# Web 界面
gradio>=5.0.0,<6.0.0

# AI 框架（Prompt 优化 + ReAct Agent）
dspy>=3.0.0,<4.0.0

# 向量数据库
chromadb>=1.0.0,<2.0.0

# 文档解析（支持 .txt / .pdf / .docx）
docling>=2.0.0,<3.0.0

# 配置文件解析（config.yaml）
PyYAML>=6.0.0,<7.0.0

# HTTP 客户端（调用 Ollama API）
requests>=2.31.0,<3.0.0

# Reranker 重排序（🟡 P1 新增）
sentence-transformers>=3.0.0,<4.0.0

gradio>=5.0.0,<6.0.0
huggingface-hub==0.36.2